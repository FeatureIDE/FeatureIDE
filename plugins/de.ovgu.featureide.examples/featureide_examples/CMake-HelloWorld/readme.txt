This is a HelloWorld example for CMake conditional compilation with the MinGW compiler.

1. Install MinGW:

https://sourceforge.net/projects/mingw/
Then add C:\MinGW\bin; to the PATH environment variable.

2. Install CMake
https://cmake.org
Then add the CMake binary to the PATH environment variable.

3. Restart your Eclipse.